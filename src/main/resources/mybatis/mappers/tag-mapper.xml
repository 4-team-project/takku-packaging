<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.takku.project.mapper.TagMapper">

	<select id="selectTagNamesByFundingId" resultType="string"
		parameterType="int">
		SELECT t.tag_name
		FROM takku_tag t
		JOIN takku_funding_tag
		ft ON t.tag_id = ft.tag_id
		WHERE ft.funding_id = #{fundingId}
	</select>

	<select id="selectTagIdByName" resultType="int">
		SELECT tag_id FROM takku_tag WHERE tag_name = #{tagName}
	</select>

	<insert id="insertTag" parameterType="TagDTO">
	<selectKey keyProperty="tagId" resultType="int" order="BEFORE">
        SELECT takku_tag_seq.NEXTVAL FROM dual
    </selectKey>
		INSERT INTO takku_tag (tag_id, tag_name)
		VALUES (#{tagId}, #{tagName})
	</insert>

	<insert id="insertFundingTag">
		INSERT INTO takku_funding_tag (funding_id, tag_id)
		VALUES (#{fundingId}, #{tagId})
	</insert>
</mapper>